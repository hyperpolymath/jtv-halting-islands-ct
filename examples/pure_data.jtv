# A standalone DATA expression. Run with:
#   python -m jtv.cli examples/pure_data.jtv --mode data
#
# Exact rational arithmetic: 1/3 + 1/3 + 1/3 == 1 exactly (no float drift),
# because DATA-language numbers are fractions.Fraction, not floats.
1/3 plus 1/3 added_to 1/3
